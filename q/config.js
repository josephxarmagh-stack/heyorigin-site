/* Public settings for the questionnaire pages. `key` is the Supabase PUBLIC (anon) key: it can only INSERT into `answers`
   (row-level security), never read. Filled in the moment the Supabase project exists (origin/os/q/README.md). */
window.Q_CONFIG = { url: "", key: "", sms: "" };
